<UserControl x:Class="GGHardware.Views.ClientesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             Background="{DynamicResource MaterialDesignBackground}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Encabezado -->
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,20">
            <Button x:Name="btnVolver" ToolTip="Volver a la vista de Registro" Click="btnVolver_Click">
                <materialDesign:PackIcon Kind="ArrowLeft" />
            </Button>
            <TextBlock Text="Gestión de Clientes"
                       FontSize="24"
                       FontWeight="SemiBold"
                       Foreground="{DynamicResource MaterialDesignBodyMediumBrush}"
                       VerticalAlignment="Center"
                       Margin="10,0,0,0"/>
        </StackPanel>

        <!-- Formulario -->
        <materialDesign:Card Grid.Row="1"
                             Background="{DynamicResource MaterialDesignCardBackground}"
                             Padding="20"
                             Margin="0,0,0,20">
            <StackPanel>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Fila 1 -->
                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Nombre:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,10,10"/>
                    <TextBox x:Name="txtNombre" Grid.Row="0" Grid.Column="1" Margin="0,0,20,10" materialDesign:HintAssist.Hint="Nombre" PreviewTextInput="txtLetras_PreviewTextInput"/>

                    <TextBlock Grid.Row="0" Grid.Column="2" Text="Apellido:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,10,10"/>
                    <TextBox x:Name="txtApellido" Grid.Row="0" Grid.Column="3" Margin="0,0,0,10" materialDesign:HintAssist.Hint="Apellido" PreviewTextInput="txtLetras_PreviewTextInput"/>

                    <!-- Fila 2 -->
                    <TextBlock Grid.Row="1" Grid.Column="0" Text="CUIT:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,10,10"/>
                    <TextBox x:Name="txtCUIT" Grid.Row="1" Grid.Column="1" Margin="0,0,20,10" materialDesign:HintAssist.Hint="CUIT" PreviewTextInput="txtNumeros_PreviewTextInput"/>

                    <TextBlock Grid.Row="1" Grid.Column="2" Text="Teléfono:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,10,10"/>
                    <TextBox x:Name="txtTelefono" Grid.Row="1" Grid.Column="3" Margin="0,0,0,10" materialDesign:HintAssist.Hint="Teléfono" PreviewTextInput="txtNumeros_PreviewTextInput"/>

                    <!-- Fila 3 -->
                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Dirección:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,10,10"/>
                    <TextBox x:Name="txtDireccion" Grid.Row="2" Grid.Column="1" Margin="0,0,20,10" materialDesign:HintAssist.Hint="Dirección"/>

                    <TextBlock Grid.Row="2" Grid.Column="2" Text="Condición Fiscal:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,10,10"/>
                    <ComboBox x:Name="cmbCondicionFiscal" Grid.Row="2" Grid.Column="3" Margin="0,0,0,10">
                        <ComboBoxItem Content="Responsable Inscripto"/>
                        <ComboBoxItem Content="Monotributista"/>
                        <ComboBoxItem Content="Exento"/>
                        <ComboBoxItem Content="Consumidor Final"/>
                    </ComboBox>

                    <!-- Fila 4 -->
                    <TextBlock Grid.Row="3" Grid.Column="0" Text="Provincia:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,10,10"/>
                    <TextBox x:Name="txtProvincia" Grid.Row="3" Grid.Column="1" Margin="0,0,20,10" materialDesign:HintAssist.Hint="Provincia" PreviewTextInput="txtLetras_PreviewTextInput"/>

                    <TextBlock Grid.Row="3" Grid.Column="2" Text="Localidad:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,10,10"/>
                    <TextBox x:Name="txtLocalidad" Grid.Row="3" Grid.Column="3" Margin="0,0,0,10" materialDesign:HintAssist.Hint="Localidad" PreviewTextInput="txtLetras_PreviewTextInput"/>
                </Grid>

                <!-- Botones -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                    <Button Content="Limpiar" Click="btnLimpiar_Click" Margin="0,0,10,0"/>
                    <Button Content="Guardar Cliente" Click="btnGuardar_Click"/>
                </StackPanel>
            </StackPanel>
        </materialDesign:Card>

        <!-- DataGrid -->
        <materialDesign:Card Grid.Row="2"
                             Background="{DynamicResource MaterialDesignCardBackground}"
                             Padding="10">
            <DataGrid x:Name="dgClientes"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      HeadersVisibility="Column"
                      GridLinesVisibility="None"
                      SelectionChanged="dgClientes_SelectionChanged">

                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding id_cliente}" Width="Auto"/>
                    <DataGridTextColumn Header="Nombre" Binding="{Binding nombre}" Width="*"/>
                    <DataGridTextColumn Header="Apellido" Binding="{Binding apellido}" Width="*"/>
                    <DataGridTextColumn Header="CUIT" Binding="{Binding CUIT}" Width="Auto"/>
                    <DataGridTextColumn Header="Condición Fiscal" Binding="{Binding condicion_fiscal}" Width="*"/>
                    <DataGridTemplateColumn Header="Acciones" Width="Auto">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="Editar" Margin="0,0,5,0" Click="btnEditar_Click"/>
                                    <Button Content="Eliminar" Click="btnEliminar_Click"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </materialDesign:Card>
    </Grid>
</UserControl>
