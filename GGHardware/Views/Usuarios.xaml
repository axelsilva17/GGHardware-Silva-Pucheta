<UserControl x:Class="GGHardware.Views.Usuarios"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             Background="{DynamicResource MaterialDesignBackground}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" VerticalAlignment="Top" Margin="0,0,0,20">
            
            <TextBlock Text="Gestión de Usuarios"
                       FontSize="24"
                       FontWeight="SemiBold"
                       Foreground="{DynamicResource MaterialDesignBodyMediumBrush}"
                       VerticalAlignment="Center"
                       Margin="10,0,0,0"/>
        </StackPanel>

        <Border Grid.Row="1"
                Background="{DynamicResource MaterialDesignCardBackground}"
                Padding="20"
                Margin="0,0,0,20">
            <StackPanel>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <materialDesign:PackIcon Grid.Row="0" Grid.Column="0" Kind="AccountCard" VerticalAlignment="Center" Margin="0,0,10,10"/>
                    <TextBox x:Name="txtDNI" Grid.Row="0" Grid.Column="1" materialDesign:HintAssist.Hint="DNI" PreviewTextInput="txtDNI_PreviewTextInput" TabIndex="0"/>

                    <materialDesign:PackIcon Grid.Row="0" Grid.Column="2" Kind="Account" VerticalAlignment="Center" Margin="20,0,10,10"/>
                    <TextBox x:Name="txtNombre" Grid.Row="0" Grid.Column="3" materialDesign:HintAssist.Hint="Nombre" PreviewTextInput="txtLetras_PreviewTextInput" TabIndex="1"/>

                    <materialDesign:PackIcon Grid.Row="1" Grid.Column="0" Kind="Account" VerticalAlignment="Center" Margin="0,0,10,10"/>
                    <TextBox x:Name="txtApellido" Grid.Row="1" Grid.Column="1" materialDesign:HintAssist.Hint="Apellido" PreviewTextInput="txtLetras_PreviewTextInput" TabIndex="2" />

                    <materialDesign:PackIcon Grid.Row="1" Grid.Column="2" Kind="Email" VerticalAlignment="Center" Margin="20,0,10,10"/>
                    <TextBox x:Name="txtCorreo" Grid.Row="1" Grid.Column="3" materialDesign:HintAssist.Hint="Correo" TabIndex="3"/>

                    <materialDesign:PackIcon Grid.Row="2" Grid.Column="0" Kind="Key" VerticalAlignment="Center" Margin="0,0,10,10"/>
                    <PasswordBox x:Name="pbContrasena" Grid.Row="2" Grid.Column="1" materialDesign:HintAssist.Hint="Contraseña" TabIndex="4"/>

                    <materialDesign:PackIcon Grid.Row="2" Grid.Column="2" Kind="Calendar" VerticalAlignment="Center" Margin="20,0,10,10"/>
                    <DatePicker x:Name="dpFechaNacimiento" Grid.Row="2" Grid.Column="3" materialDesign:HintAssist.Hint="Fecha de Nacimiento" TabIndex="5"/>

                    <materialDesign:PackIcon Grid.Row="3" Grid.Column="0" Kind="AccountGroup" VerticalAlignment="Center" Margin="0,0,10,10"/>
                    <ComboBox x:Name="cmbRol" Grid.Row="3" Grid.Column="1" materialDesign:HintAssist.Hint="Rol"
                    SelectedValuePath="Tag" DisplayMemberPath="Content" TabIndex="6" SelectionChanged="cmbRol_SelectionChanged">
                        <ComboBoxItem Content="Supervisor" Tag="1"/>
                        <ComboBoxItem Content="Usuario" Tag="2"/>
                    </ComboBox>

                </Grid>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                    <Button x:Name="btnLimpiar" Content="Limpiar" Width="100" Margin="0,0,10,0" Click="btnLimpiar_Click" TabIndex="7"/>
                    <Button x:Name="btnGuardar" Content="Guardar" Width="100" Click="btnGuardar_Click" TabIndex="8"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <Border Grid.Row="2"
                Background="{DynamicResource MaterialDesignCardBackground}"
                Padding="10">
            <DataGrid x:Name="dgUsuarios"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      HeadersVisibility="Column"
                      GridLinesVisibility="None"
                      Style="{DynamicResource MaterialDesignDataGrid}">

                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding id_usuario}" Width="60"/>
                    <DataGridTextColumn Header="DNI" Binding="{Binding dni}" Width="120"/>
                    <DataGridTextColumn Header="Nombre" Binding="{Binding Nombre}" Width="200"/>
                    <DataGridTextColumn Header="Apellido" Binding="{Binding apellido}" Width="200"/>
                    <DataGridTextColumn Header="Rol" Binding="{Binding NombreRol}" Width="150"/>
                    
                    <!-- columna de Estado-->
                    <DataGridTemplateColumn Header="Estado" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Ellipse Width="10" Height="10" VerticalAlignment="Center" Margin="0,0,5,0">
                                        <Ellipse.Style>
                                            <Style TargetType="Ellipse">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Activo}" Value="True">
                                                        <Setter Property="Fill" Value="#4CAF50"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Activo}" Value="False">
                                                        <Setter Property="Fill" Value="#F44336"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Ellipse.Style>
                                    </Ellipse>
                                    <TextBlock VerticalAlignment="Center">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="FontWeight" Value="SemiBold"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Activo}" Value="True">
                                                        <Setter Property="Text" Value="Activo"/>
                                                        <Setter Property="Foreground" Value="#4CAF50"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Activo}" Value="False">
                                                        <Setter Property="Text" Value="Inactivo"/>
                                                        <Setter Property="Foreground" Value="#F44336"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    
                    <!-- Columna de Acciones-->
                    <DataGridTemplateColumn Header="Acciones" Width="120">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button ToolTip="Editar" 
                                            Click="btnEditar_Click"
                                            Style="{StaticResource MaterialDesignIconButton}"
                                            Margin="2,0">
                                        <materialDesign:PackIcon Kind="Pencil" />
                                    </Button>

                                    <Button Click="btnActivarDesactivar_Click"
                                            Margin="2,0">
                                        <Button.Style>
                                            <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignIconButton}">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Activo}" Value="True">
                                                        <Setter Property="ToolTip" Value="Desactivar usuario"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Activo}" Value="False">
                                                        <Setter Property="ToolTip" Value="Activar usuario"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                        <materialDesign:PackIcon>
                                            <materialDesign:PackIcon.Style>
                                                <Style TargetType="materialDesign:PackIcon">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Activo}" Value="True">
                                                            <Setter Property="Kind" Value="PowerOff"/>
                                                            <Setter Property="Foreground" Value="#F44336"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Activo}" Value="False">
                                                            <Setter Property="Kind" Value="PowerOn"/>
                                                            <Setter Property="Foreground" Value="#4CAF50"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </materialDesign:PackIcon.Style>
                                        </materialDesign:PackIcon>
                                    </Button>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>