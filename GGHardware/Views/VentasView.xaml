<UserControl x:Class="GGHardware.Views.VentasView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:GGHardware.Converters"
             mc:Ignorable="d"
             Background="{DynamicResource Background}"
             MinHeight="460" MinWidth="1000">

    <UserControl.Resources>
        <!-- Converter para visibilidad -->
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <local:NullToBooleanConverter x:Key="NullToBooleanConverter"/>

        <Style x:Key="RightAlignedText" TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Right"/>
        </Style>

        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource Surface}"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="BorderBrush" Value="{DynamicResource PrimaryBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Black" Direction="270" ShadowDepth="2" Opacity="0.1" BlurRadius="8"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource PrimaryBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource PrimaryForegroundBrush}"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Margin" Value="5,0,0,0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.8"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Opacity" Value="0.7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Margin" Value="5,0,0,0"/>
            <Setter Property="BorderBrush" Value="{DynamicResource PrimaryBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource PrimaryBrush}"/>
                                <Setter Property="Foreground" Value="{DynamicResource PrimaryForegroundBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="{DynamicResource PrimaryBrush}"/>
                                <Setter Property="Foreground" Value="{DynamicResource PrimaryForegroundBrush}"/>
                                <Setter Property="Opacity" Value="0.8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SmallButton" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource PrimaryBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource PrimaryForegroundBrush}"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Margin" Value="2,0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="3"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DangerButton" TargetType="Button">
            <Setter Property="Background" Value="#D32F2F"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="Margin" Value="5,0,0,0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="3"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PlaceholderTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="{DynamicResource Background}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource PrimaryBrush}"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <Grid>
                                <ScrollViewer x:Name="PART_ContentHost"
                                            Padding="{TemplateBinding Padding}"/>
                                <TextBlock x:Name="PlaceholderText"
                                        Text="Buscar Cliente (CUIT/Nombre)"
                                         Foreground="Gray"
                                         Padding="{TemplateBinding Padding}"
                                         IsHitTestVisible="False"
                                         VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="PlaceholderText" Property="Visibility" Value="Collapsed"/>
                            </Trigger>
                            <Trigger Property="Text" Value="">
                                <Setter TargetName="PlaceholderText" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="Text" Value="{x:Null}">
                                <Setter TargetName="PlaceholderText" Property="Visibility" Value="Visible"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernComboBox" TargetType="ComboBox">
            <Setter Property="Background" Value="{DynamicResource Background}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource PrimaryBrush}"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Selección de Productos -->
        <Border Grid.Column="0" Style="{StaticResource CardStyle}" Margin="0,0,10,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Header con título y botón de historial -->
                <Grid Grid.Row="0" Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0"
                               Text="Selección de Productos"
                               FontSize="20"
                               FontWeight="SemiBold"
                               Foreground="{DynamicResource TextPrimary}"
                               VerticalAlignment="Center"/>

                    <Button Grid.Column="1"
                            Content="📋 Ver Historial"
                            Style="{StaticResource SecondaryButton}"
                            Click="VerHistorial_Click"
                            ToolTip="Ver historial de ventas"/>
                </Grid>

                <!--  Panel de búsqueda por código -->
                <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
                    <TextBlock Text="Código:" 
                               VerticalAlignment="Center" 
                               FontWeight="SemiBold" 
                               Margin="0,0,10,0"
                               Foreground="{DynamicResource TextPrimary}"/>
                    <TextBox x:Name="txtCodigoProducto" 
                             Text="{Binding CodigoProductoBuscar, UpdateSourceTrigger=PropertyChanged}"
                             Width="200"
                             KeyDown="TxtCodigoProducto_KeyDown"
                             Background="{DynamicResource Background}"
                             BorderBrush="{DynamicResource PrimaryBrush}"
                             BorderThickness="2"
                             Padding="8"
                             ToolTip="Código de barras, interno o ID del producto"/>
                    <Button Content="Buscar" 
                            Style="{StaticResource SmallButton}"
                            Click="BuscarProducto_Click"
                            Margin="10,0,0,0"/>
                </StackPanel>

                <DataGrid x:Name="dgProductos"
                          Grid.Row="2"
                          ItemsSource="{Binding Productos}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          SelectionMode="Single"
                          Background="{DynamicResource Surface}"
                          Foreground="{DynamicResource TextPrimary}"
                          BorderThickness="0"
                          RowBackground="{DynamicResource Surface}"
                          AlternatingRowBackground="{DynamicResource Background}"
                          GridLinesVisibility="Horizontal"
                          HorizontalGridLinesBrush="{DynamicResource TextSecondary}">

                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="{DynamicResource PrimaryBrush}"/>
                            <Setter Property="Foreground" Value="{DynamicResource PrimaryForegroundBrush}"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="Padding" Value="12,8"/>
                            <Setter Property="BorderThickness" Value="0,0,1,0"/>
                            <Setter Property="BorderBrush" Value="{DynamicResource PrimaryForegroundBrush}"/>
                            <Setter Property="HorizontalContentAlignment" Value="Left"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>

                    <DataGrid.CellStyle>
                        <Style TargetType="DataGridCell">
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Padding" Value="12,8"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource PrimaryBrush}"/>
                                    <Setter Property="Foreground" Value="{DynamicResource PrimaryForegroundBrush}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.CellStyle>

                    <DataGrid.RowStyle>
                        <Style TargetType="DataGridRow">
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource PrimaryBrush}"/>
                                    <Setter Property="Foreground" Value="{DynamicResource PrimaryForegroundBrush}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.RowStyle>

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Nombre" Binding="{Binding Nombre}" Width="*"/>
                        <DataGridTextColumn Header="Código" Binding="{Binding codigo_interno}" Width="80"/>
                        <DataGridTextColumn Header="Precio" Binding="{Binding precio_venta, StringFormat=C}" Width="Auto"
                                            ElementStyle="{StaticResource RightAlignedText}"/>
                        <DataGridTextColumn Header="Stock" Binding="{Binding Stock}" Width="Auto"
                                            ElementStyle="{StaticResource RightAlignedText}"/>
                        <DataGridTemplateColumn Header="Acción" Width="Auto">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Agregar"
                                            Style="{StaticResource PrimaryButton}"
                                            Click="AgregarProducto_Click"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <!-- Información del Cliente y Carrito -->
        <Border Grid.Column="1" Style="{StaticResource CardStyle}" Margin="10,0,0,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           Text="Información del Cliente"
                           FontSize="20"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource TextPrimary}"
                           Margin="0,0,0,10"/>

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" MaxHeight="200">
                    <StackPanel>
                        <TextBox x:Name="txtBuscarCliente"
                                 Style="{StaticResource PlaceholderTextBox}"
                                 Margin="0,0,0,10" 
                                 TextChanged="txtBuscarCliente_TextChanged"
                                 GotFocus="TxtBuscarCliente_GotFocus"
                                 LostFocus="TxtBuscarCliente_LostFocus"/>

                        <TextBlock Text="Tipo de Comprobante:" 
                                   FontWeight="SemiBold" 
                                   Margin="0,0,0,5"
                                   Foreground="{DynamicResource TextPrimary}"/>
                        <ComboBox x:Name="cmbTipoComprobante" 
                                  ItemsSource="{Binding TiposComprobante}"
                                  SelectedItem="{Binding TipoComprobanteSeleccionado}"
                                  DisplayMemberPath="Nombre"
                                  Style="{StaticResource ModernComboBox}"
                                  Margin="0,0,0,10"/>

                        <TextBlock Text="Método de Pago:" 
                                   FontWeight="SemiBold" 
                                   Margin="0,0,0,5"
                                   Foreground="{DynamicResource TextPrimary}"/>
                        <ComboBox x:Name="cmbMetodoPago"
                                  ItemsSource="{Binding MetodosPago}"
                                  SelectedItem="{Binding MetodoPagoSeleccionado}"
                                  DisplayMemberPath="nombre"
                                  Style="{StaticResource ModernComboBox}"
                                  Margin="0,0,0,10"/>

                        <StackPanel Visibility="{Binding MostrarCampoVuelto, Converter={StaticResource BoolToVisibilityConverter}}">
                            <TextBlock Text="Monto Recibido:" 
                                       FontWeight="SemiBold" 
                                       Margin="0,0,0,5"
                                       Foreground="{DynamicResource TextPrimary}"/>
                            <TextBox x:Name="txtMontoRecibido" 
                                     Text="{Binding MontoRecibido, UpdateSourceTrigger=PropertyChanged}"
                                     Background="{DynamicResource Background}"
                                     BorderBrush="{DynamicResource PrimaryBrush}"
                                     BorderThickness="2"
                                     Padding="8"
                                     Margin="0,0,0,5"/>
                            <TextBlock Text="{Binding Vuelto, StringFormat='Vuelto: {0:C}'}" 
                                       FontWeight="Bold" 
                                       Foreground="Green"
                                       Margin="0,0,0,10"/>
                        </StackPanel>

                        <TextBlock Text="Observaciones:" 
                                   FontWeight="SemiBold" 
                                   Margin="0,0,0,5"
                                   Foreground="{DynamicResource TextPrimary}"/>
                        <TextBox x:Name="txtObservaciones" 
                                 Text="{Binding Observaciones, UpdateSourceTrigger=PropertyChanged}"
                                 Background="{DynamicResource Background}"
                                 BorderBrush="{DynamicResource PrimaryBrush}"
                                 BorderThickness="2"
                                 Padding="8"
                                 Height="60"
                                 TextWrapping="Wrap"
                                 AcceptsReturn="True"
                                 VerticalScrollBarVisibility="Auto"
                                 Margin="0,0,0,15"/>
                    </StackPanel>
                </ScrollViewer>

                <StackPanel Grid.Row="2">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <TextBlock Text="Productos en Carrito"
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource TextPrimary}"
                                   VerticalAlignment="Center"/>

                        <Button Content="Desc. 5%" 
                                Style="{StaticResource SmallButton}"
                                Background="Orange"
                                Click="AplicarDescuento5_Click"
                                Margin="10,0,0,0"/>
                        <Button Content="Desc. 10%" 
                                Style="{StaticResource SmallButton}"
                                Background="Orange"
                                Click="AplicarDescuento10_Click"/>
                        <Button Content="Desc. 15%" 
                                Style="{StaticResource SmallButton}"
                                Background="Orange"
                                Click="AplicarDescuento15_Click"/>
                    </StackPanel>

                    <Border Background="{DynamicResource Background}"
                            BorderBrush="{DynamicResource PrimaryBrush}"
                            BorderThickness="1"
                            CornerRadius="4"
                            MinHeight="150"
                            MaxHeight="200">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <ListBox x:Name="lstCarrito"
                                     ItemsSource="{Binding Carrito}"
                                     Background="Transparent"
                                     BorderThickness="0"
                                     ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="{DynamicResource Surface}"
                                                Padding="6"
                                                Margin="2"
                                                CornerRadius="3"
                                                BorderBrush="{DynamicResource TextSecondary}"
                                                BorderThickness="1">
                                            <Grid>
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto"/>
                                                </Grid.RowDefinitions>

                                                <TextBlock Grid.Row="0" 
                                                           Text="{Binding Nombre}"
                                                           Foreground="{DynamicResource TextPrimary}"
                                                           FontWeight="SemiBold"
                                                           TextWrapping="Wrap"
                                                           Margin="0,0,0,5"/>

                                                <Grid Grid.Row="1">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>

                                                    <StackPanel Grid.Column="0" 
                                                                Orientation="Horizontal">
                                                        <Button Content="-"
                                                                Width="20"
                                                                Height="20"
                                                                FontSize="12"
                                                                FontWeight="Bold"
                                                                Background="#FF6B6B"
                                                                Foreground="White"
                                                                BorderThickness="0"
                                                                Cursor="Hand"
                                                                Margin="0,0,2,0"
                                                                Click="QuitarUnaUnidad_Click">
                                                            <Button.Style>
                                                                <Style TargetType="Button">
                                                                    <Setter Property="Template">
                                                                        <Setter.Value>
                                                                            <ControlTemplate TargetType="Button">
                                                                                <Border Background="{TemplateBinding Background}"
                                                                                        CornerRadius="3">
                                                                                    <ContentPresenter HorizontalAlignment="Center"
                                                                                                    VerticalAlignment="Center"/>
                                                                                </Border>
                                                                                <ControlTemplate.Triggers>
                                                                                    <Trigger Property="IsMouseOver" Value="True">
                                                                                        <Setter Property="Opacity" Value="0.8"/>
                                                                                    </Trigger>
                                                                                </ControlTemplate.Triggers>
                                                                            </ControlTemplate>
                                                                        </Setter.Value>
                                                                    </Setter>
                                                                </Style>
                                                            </Button.Style>
                                                        </Button>

                                                        <TextBlock Text="{Binding Cantidad}" 
                                                                   Width="20"
                                                                   HorizontalAlignment="Center"
                                                                   TextAlignment="Center"
                                                                   Foreground="{DynamicResource TextPrimary}"
                                                                   FontWeight="SemiBold"
                                                                   VerticalAlignment="Center"
                                                                   Margin="2,0"/>

                                                        <Button Content="+"
                                                                Width="20"
                                                                Height="20"
                                                                FontSize="12"
                                                                FontWeight="Bold"
                                                                Background="{DynamicResource PrimaryBrush}"
                                                                Foreground="{DynamicResource PrimaryForegroundBrush}"
                                                                BorderThickness="0"
                                                                Cursor="Hand"
                                                                Margin="2,0,0,0"
                                                                Click="AgregarUnaUnidad_Click">
                                                            <Button.Style>
                                                                <Style TargetType="Button">
                                                                    <Setter Property="Template">
                                                                        <Setter.Value>
                                                                            <ControlTemplate TargetType="Button">
                                                                                <Border Background="{TemplateBinding Background}"
                                                                                        CornerRadius="3">
                                                                                    <ContentPresenter HorizontalAlignment="Center"
                                                                                                    VerticalAlignment="Center"/>
                                                                                </Border>
                                                                                <ControlTemplate.Triggers>
                                                                                    <Trigger Property="IsMouseOver" Value="True">
                                                                                        <Setter Property="Opacity" Value="0.8"/>
                                                                                    </Trigger>
                                                                                </ControlTemplate.Triggers>
                                                                            </ControlTemplate>
                                                                        </Setter.Value>
                                                                    </Setter>
                                                                </Style>
                                                            </Button.Style>
                                                        </Button>
                                                    </StackPanel>

                                                    <StackPanel Grid.Column="1" 
                                                                Orientation="Vertical"
                                                                Margin="10,0">
                                                        <TextBlock Text="{Binding Precio, StringFormat=C}" 
                                                                   FontSize="11"
                                                                   Foreground="{DynamicResource TextPrimary}"/>
                                                        <TextBlock Text="{Binding PorcentajeDescuento, StringFormat='Desc: {0:F1}%'}" 
                                                                   FontSize="10"
                                                                   Foreground="Orange"
                                                                   Visibility="{Binding TieneDescuento, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                                    </StackPanel>

                                                    <TextBlock Grid.Column="2" 
                                                               Text="{Binding Subtotal, StringFormat=C}" 
                                                               Margin="5,0"
                                                               Foreground="{DynamicResource PrimaryBrush}"
                                                               FontWeight="Bold"
                                                               VerticalAlignment="Center"/>

                                                    <Button Grid.Column="3"
                                                            Content="×"
                                                            Style="{StaticResource DangerButton}"
                                                            Width="18"
                                                            Height="18"
                                                            FontSize="12"
                                                            ToolTip="Eliminar producto del carrito"
                                                            Click="QuitarProducto_Click"/>
                                                </Grid>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </ScrollViewer>
                    </Border>
                </StackPanel>

                <Border Grid.Row="3"
                        Background="{DynamicResource PrimaryBrush}"
                        CornerRadius="4"
                        Padding="15"
                        Margin="0,28,0,0">
                    <Grid Margin="0,-35,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0"
                                   Text="{Binding Total, StringFormat='Total: {0:C}'}"
                                   FontWeight="Bold"
                                   FontSize="18"
                                   Foreground="{DynamicResource PrimaryForegroundBrush}"
                                   HorizontalAlignment="Center"
                                   Margin="0,0,0,10"/>

                        <StackPanel Grid.Row="1"
                                    Orientation="Horizontal"
                                    HorizontalAlignment="Center">
                            <Button Content="Cancelar" 
                                    Style="{StaticResource SecondaryButton}"
                                    Background="Transparent"
                                    BorderBrush="White"
                                    Foreground="White"
                                    Width="90"
                                    Click="CancelarVenta_Click"/>
                            <Button Content="Finalizar Venta" 
                                    Style="{StaticResource SecondaryButton}"
                                    Background="White"
                                    BorderBrush="White"
                                    Foreground="{DynamicResource PrimaryBrush}"
                                    Width="120"
                                    Click="FinalizarVenta_Click"/>



                        </StackPanel>
                      
                    </Grid>
                    
                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>