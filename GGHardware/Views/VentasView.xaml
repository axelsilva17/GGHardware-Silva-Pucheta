<UserControl x:Class="GGHardware.Views.VentasView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Background="White"
             MinHeight="460" MinWidth="830">

    <UserControl.Resources>
        <Style x:Key="RightAlignedText" TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Right"/>
        </Style>

        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="#F5F5F5"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="BorderBrush" Value="#DDD"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#1976D2"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Margin" Value="5,0,0,0"/>
            <Setter Property="BorderBrush" Value="#1976D2"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="#E0E0E0"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="BorderBrush" Value="#CCC"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Selección de Productos -->
        <Border Grid.Column="0" Style="{StaticResource CardStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           Text="Selección de Productos"
                           FontSize="20"
                           FontWeight="SemiBold"
                           Margin="0,0,0,10"/>

                <DataGrid x:Name="dgProductos"
                          Grid.Row="1"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          SelectionMode="Single">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Nombre" Binding="{Binding Nombre}" Width="*"/>
                        <DataGridTextColumn Header="Precio" Binding="{Binding Precio, StringFormat=C}" Width="Auto"
                                            ElementStyle="{StaticResource RightAlignedText}"/>
                        <DataGridTextColumn Header="Stock" Binding="{Binding Stock}" Width="Auto"
                                            ElementStyle="{StaticResource RightAlignedText}"/>
                        <DataGridTemplateColumn Header="Acción" Width="Auto">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Agregar"
                                            Style="{StaticResource PrimaryButton}"
                                            Command="{Binding DataContext.AgregarProductoCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <!-- Información del Cliente y Carrito -->
        <Border Grid.Column="1" Style="{StaticResource CardStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           Text="Información del Cliente"
                           FontSize="20"
                           FontWeight="SemiBold"
                           Margin="0,0,0,10"/>

                <!-- Hint simulado para WPF -->
                <TextBox x:Name="txtBuscarCliente"
         Text="Buscar Cliente (DNI/Nombre)"
         Foreground="Gray"/>


                <StackPanel Grid.Row="2">
                    <TextBlock Text="Productos en Carrito"
                               FontWeight="SemiBold"
                               Margin="0,10,0,10"/>
                    <ListBox x:Name="lstCarrito">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,0,0,5">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="{Binding Nombre}"/>
                                    <TextBlock Grid.Column="1" Text="{Binding Cantidad}" Margin="10,0"/>
                                    <TextBlock Grid.Column="2" Text="{Binding Precio, StringFormat=C}" Margin="10,0"/>
                                    <Button Grid.Column="3"
                                            Content="X"
                                            Style="{StaticResource SecondaryButton}"
                                            Command="{Binding DataContext.QuitarProductoCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"/>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </StackPanel>

                <StackPanel Grid.Row="3"
                            Orientation="Horizontal"
                            HorizontalAlignment="Right"
                            Margin="0,20,0,0"
                            VerticalAlignment="Center">
                    <TextBlock Text="{Binding Total, StringFormat=C}"
                               FontWeight="Bold"
                               FontSize="18"
                               VerticalAlignment="Center"
                               Margin="0,0,20,0"/>
                    <Button Content="Cancelar" Style="{StaticResource SecondaryButton}"/>
                    <Button Content="Finalizar Venta" Style="{StaticResource PrimaryButton}" Width="120"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
