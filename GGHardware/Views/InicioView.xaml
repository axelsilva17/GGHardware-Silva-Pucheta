<UserControl x:Class="GGHardware.Views.InicioView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Background="#F3F4F6">

    <Viewbox Stretch="Uniform">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="10" HorizontalAlignment="Center">
                <TextBlock Text="GGHardware" FontSize="32" FontWeight="Bold" Foreground="#0EA5E9" TextAlignment="Center"/>
                <TextBlock Text="Tu tienda de hardware de confianza" FontSize="16" Foreground="Gray" TextAlignment="Center"/>
            </StackPanel>

            <StackPanel Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="20" Width="300">
                <Button Margin="0,10,0,0" Padding="10" Background="#0EA5E9" Foreground="White">
                    Inicio Sesion
                </Button>
                <Button Margin="0,10,0,0" Padding="10" Background="#1E293B" Foreground="White">
                    Registrarse
                </Button>
                <Button Margin="0,10,0,0" Padding="10" Background="#CBD5E1" Foreground="Black">
                    Entrar como Invitado
                </Button>
            </StackPanel>

            <TextBlock Grid.Row="2" Text="2025 GGHardware"
                       HorizontalAlignment="Center" Margin="10" Foreground="Gray"/>
        </Grid>
    </Viewbox>
</UserControl>